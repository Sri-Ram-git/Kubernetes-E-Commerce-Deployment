apiVersion: v1
kind: Service

metadata:
  name: ecommerce-service

spec:
  selector:
    app: ecommerce-site

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007

  type: NodePort